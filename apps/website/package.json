{
  "name": "website",
  "scripts": {
    "prepare": "node ../../tools/scripts/changelog/cli.mjs",
    "dev": "pnpm prepare && vitepress dev docs",
    "build": "pnpm prepare && vitepress build docs"
  }
}

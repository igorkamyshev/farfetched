import { Barrier } from '@farfetched/core';

import { ChainProtocol } from './chain.ptorocol';

export function barrierChain(barrier: Barrier): ChainProtocol<any> {
  throw new Error('not implemented');
}
